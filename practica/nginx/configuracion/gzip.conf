# Gzip configuration for the exercise (do not set `gzip on;` if already configured elsewhere)
gzip on;
gzip_types  text/css application/javascript application/json text/plain;
gzip_comp_level 5;
gzip_vary on;
gzip_min_length 256;
